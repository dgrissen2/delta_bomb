## Claude Review — Verdict: CONDITIONAL PASS

**Scope**: changes in outputs/b06_iv_expansion_150d_2026-09-19/recalculate.py
**Files reviewed**: 1

### Findings

| # | Finding | Severity | File:Line | Description |
|---|---------|----------|-----------|-------------|
| 1 | Rerunning `features` overwrites frozen artifacts, then always fails | MEDIUM | recalculate.py:302-328 (also 245-247, 304) | `feature_stage` has no early return once the freeze exists. It first rewrites `sector_event_features.parquet`, `basket_before_outcomes.csv`, `surface_coverage.csv` and `missing_panels.csv`. It then calls `freeze_json`, whose payload includes a wall-clock `"at"`, so on any rerun the payload differs and it raises "Frozen artifact differs". Separately, `surface_coverage.csv` rows follow thread completion order (`records` is filled from `as_completed`). The saved file is out of sorted order (…, XLY, XLE, XLP, …), so a rerun can produce different bytes. `analyze_stage` would then fail `verify_hashes(frozen["hashes"])`. **Not reached in this run:** all hashes verify. However, rerunning `features` would damage the frozen archive rather than check it. |
| 2 | Archive code runs before its hash is checked | LOW | iv_rules.py:18-26 | `exec` compiles functions taken from `evidence.json` when the module is imported. The archive's hash is only checked later, in `feature_stage` and `analyze_stage`, and `SOURCE_SHA256` is printed but never compared with a pinned value. The file is trusted and local, but a modified archive would run before detection. The current archive hash matches `parent_freeze.json`. |
| 3 | Original-cohort missing-panel reason is hard-coded | LOW | recalculate.py:114 | Every absent original surface is labelled `missing_tenor_bracket` whatever the real cause. All 13 cases (XLRE) are genuinely `missing_tenor_bracket` in the old manifest, so the label is correct for this run but not in general. |
| 4 | Completeness is not asserted at the stage boundaries | LOW | recalculate.py:143, 262-274, 366 | 1. `ready_jobs` does not check that every one of the 150×11 date/sector cells is either a job or a missing-panel record. A selection absent from the manifest would silently become "unavailable" and never appear in `missing_panels.csv`. 2. `pd.concat(frames)` raises if `successful` is empty. 3. The inner join at line 366 has no `len(joined)==4*len(parents)` check, which the frozen `replication.py` did have. **Verified for this run:** 1,615 ok + 35 missing = 1,650, the ledger has 4,160 rows, and 0 outcomes are null. |
| 5 | Only `ValueError` is caught per panel; the frozen rules raise `AssertionError` | LOW | recalculate.py:172; evidence.json `experiment_source` L34, L61 | `make_surface` contains `assert len(lower) and len(upper)`, and `add_quality` also asserts. A panel whose data does not bracket 30 DTE (for example, one expiry returning zero rows) would crash the whole thread pool instead of being recorded as `invalid_source`. Under `python -O` the assert is stripped and an `IndexError` occurs instead. **Not reached:** 0 of 3,882 responses had zero rows (the smallest had 12,600), and all 1,078 selected new panels succeeded. |
| 6 | 09:30 is always missing, so 09:30–09:59 windows are always "unknown" (inherited frozen rule) | LOW | recalculate.py:204-206 | The 09:30 IV sample is NaN in 100% of 400 sampled panels; no panel reaches 300/300 valid minutes. The reference window for parent_min=605 starts at 09:30, so all 18 such parents (3 original, 15 additional) are "unknown" in all four variants. They stay in the baseline but can never enter an IV row. The affected share differs by cohort (2.3% vs 0.8%). This is the frozen rule working as written, not a `recalculate.py` defect. It is worth disclosing because it slightly changes which events each comparison contains. |
| 7 | Original-validation check skips first and second slopes | LOW | recalculate.py:289 | Only `available` and `acceleration` are compared per event. `falling` depends on `second_slope` and is checked only through the aggregate state counts at lines 293-300. **I verified per event** that both slopes match the saved features to 1e-15 across 4,114 rows, so this is a weakness in the guard, not an error. |
| 8 | Original-parent reproduction check only works in one direction | LOW | recalculate.py:81-82 | `.loc[old.index]` always returns `len(old)` rows, so `len(reproduction) != len(old)` can never fail. Extra original-cohort parents would go undetected. **Verified for this run:** the original-cohort IDs equal the old 374 IDs exactly. |
| 9 | No table-reconciliation test | LOW | test_expansion.py | `PROTOCOL.md` requires tests for table reconciliation and incomplete samples. Neither the cohort table ↔ ledger ↔ bootstrap consistency nor the `feature_stage` population accounting is covered. My external reconciliation passed. |
