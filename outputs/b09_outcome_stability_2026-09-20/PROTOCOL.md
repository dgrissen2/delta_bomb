# Outcome-table confidence intervals, Charlie lens, then day deletion

User-authorized follow-up September20,2026. Fixed baseline B09F4-or-SPX and
combined B09/B05/B07 union, unspaced, unchanged685unique timestamps and239research
dates in2025–September18,2026. Native+5 first versus entry-10 in60minute bars,
strict causal above-VT. Unresolved signed endpoint buckets retain the preceding
2.5point edges and separate zero. This changes no entry or outcome definition.

Execution order:
1. Verify source hashes and attach existing neither endpoints to the fixed ledger.
   Recalculate baseline/combined winner-rate95%intervals using5000paired whole-date
   multinomial resamples,seed20260920,within each half/pooled calendar including
   zero-entry dates. Independently reconcile all ten intervals with prior results.
   Publish the full outcome tables with intervals before starting new deletion work.
2. Interpret those full-sample tables through the registry-resolved canonical
   Charlie lens, applied locally. Side-conversation constraints prohibit separate
   reviewer agents. This is not the real Charlie or an independent reviewer approval.
   No conclusions about unmeasured dealer positioning or actual option profits.
3. Remove each research date in turn and recompute both complete outcome mixes
   and winner rates, without retuning/reselecting entries. Reconcile all956winner
   count/rate rows against the existing union-study deletion ledger. Show pooled
   and half-year win-rate ranges, most damaging date removal, stop-rate ranges,
   largest/top-five winner-date concentration, and joint combined-minus-baseline
   changes when the same date is removed from both. Zero-entry dates remain in
   the ledger; they do not affect extrema. Preserve denominator changes.

All outcome cells use total signals in that row as denominator; buckets apply
only to neither cases. Every event belongs to exactly one category, and rates
sum to100before rounding. An empty sample has no rate, never zero accuracy.

Intervals allow dependence among signals on the same date, but do not adjust for
the earlier rule search or dependence between dates. Sparse partial2026H2 bootstrap
results are fragile; omit no-signal resamples and report their count. Deletion
extrema are sensitivity ranges, not confidence intervals. One-day robustness is
not proof against a multi-day regime shift or forward accuracy. No new threshold,
spacing, provider calls, test-data collection, or production changes.

New tables only in central_trade_data/thetadata/b09_outcome_stability_2026-09-20-v1.
Code, full tables, Charlie interpretation and findings in the matching project
output directory. Source files and unrelated concurrent work remain unchanged.
