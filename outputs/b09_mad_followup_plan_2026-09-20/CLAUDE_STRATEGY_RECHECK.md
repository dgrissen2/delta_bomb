## Claude Strategy Review — Generic Logic
**Verdict**: CONDITIONAL PASS (7 findings)

### Findings

| # | Finding | Severity | Category | Details |
|---|---------|----------|----------|---------|
| 1 | The newly added B09 sign-only controls are ~10× less selective than the rules they are meant to attribute, so they cannot separate "magnitude" from "strictness" | HIGH | ineffective-control | BLOCKING for the attribution claim; cheap to fix. Replayed read-only from the frozen `sector_at_entry.parquet` + `events_with_outcomes.parquet` (my replay reproduces S4 235/139 and F4 188/115 and their no/unknown groups exactly, so the membership logic is trustworthy). The proposed control `a<0 in ≥4 of 11` qualifies **2,544/3,141 = 81.0%** of entries; `a<0 AND b2<−1e−12 in ≥4` qualifies **1,933/3,141 = 61.5%**. S4 retains 235 (7.5%), F4 188 (6.0%) — a 10.3–10.8× selectivity gap. Their hit rates are **54.17%** and **54.11%** against the 54.25% parent, i.e. the controls *are* the parent. The planned contrast therefore credits the MAD magnitude requirement for an effect that is inseparable from a 10× change in strictness, and its result is already determined before the cells are run. Two of ten declared cells buy nothing. The attribution questions that are actually open are (a) magnitude vs. strictness — control the sign rule at a breadth that retains ≈235/188 entries — and (b) MAD normalisation vs. raw magnitude — a raw \|acceleration\| threshold at matched retention. Only (b) isolates the genuinely novel element. (Disclosure: this was a read-only groupby on frozen artifacts, no fetch, no file change; it does pre-empt the two B09 control cells.) |
| 2 | Sector coverage is not stable across half-years, and the largest uplift half is the worst-coverage half — the confound is shared by all ten planned cells | HIGH | measurement-confound | BLOCKING for the half-year stability criterion; not flagged in either document. S4 unknown rate by half: **4.3% / 4.4% / 13.1% / 4.7%** (2025 H1, H2, 2026 H1, H2); F4: 3.9 / 3.4 / 11.7 / 4.1%. Driver: XLRE observed on **35.4%** of 2026 H1 entries vs 70.9% in 2025 H1 (XLU 87.8%, XLC 93.2% in the same half); mean observed sectors 10.12 vs ~10.6. Because qualification uses bounded counting on a fixed denominator of 11 — "yes" requires ≥4 *observed* qualifiers — lower coverage makes the rule strictly harder to satisfy. 2026 H1 is exactly where uplift is largest (S4 +5.6pp, F4 +8.7pp). So "positive uplift in every completed half" — the criterion that picked these 4 cells from 48, and that contract item 9 re-applies to all ten transfer cells — compares non-exchangeable measurement regimes. Worse for the round's purpose: the coverage pattern is a property of the sector data, not the parent, so it reproduces identically on B05 and B07 and would read as successful portability. Fix before running: publish per-half coverage/unknown rates beside every cell, and pre-declare a coverage-held-constant sensitivity (restrict to entries with all 11, or a fixed subset, observed). |
| 3 | Item 9's pre-declared reading rule is not power-aware; on B07 "zero cells" is the likely outcome even if the effect is real | HIGH | underpowered-decision-rule | The author rejected an N floor (response #4). The residual problem is not a floor, it is that a fixed interpretation is pre-committed at an N where it does not discriminate. Retention transfers near 1:1 across parents — from the cited `comparison.csv`, B07/B09 entry-retention ratios across 8 analogue IV rules are 0.5–1.1 (`original_accelerating_6` 21.9% vs 21.8%; `guarded_100_accelerating_6` 15.7% vs 14.0%; `original_falling_8` 10.1% vs 17.8%). Applying S4/F4's B09 retention (7.5%/6.0%) to B07's 415 entries gives pooled N ≈ **16–35**, ≈5–11 per completed half. At n≈6–10 per half with a true +5pp effect, P(observed half beats the parent half) ≈ 0.54–0.55, so P(all three halves positive) ≈ **0.16 — against ≈0.09 under the null**. The rule separates a real transfer from noise by ~7pp, yet the plan pre-commits to reading zero passes as "the pattern did not transfer." Add an explicit *underpowered / uninformative* outcome, or make pooled-halves the primary readout with the per-half pattern as sensitivity. |
| 4 | The largest and most certain lever on the stated "retained opportunities" objective is excluded from the round while a 741-session SPX backfill is not | HIGH | missing-alternative | S4's unknown pool is **212 entries — 90% the size of its entire 235-entry qualifying set — hitting 61.3%**, at or above the qualifying group. **107 of those 212 are unknown solely because XLRE is missing**, and XLRE alone is 1,235 of the 1,688 missing ETF-entry slots (73%). Resolving coverage converts those entries to yes/no on the *same* frozen rule, with no new parent, no new threshold, and no new multiplicity; an unknown entry with one missing sector has exactly 3 observed qualifiers, so a material share would resolve to "yes". XLRE coverage of 70.9% → 71.7% → 35.4% → 55.6% across halves does not look like a permanent market feature, which suggests a collection or quote-policy artifact rather than an unfixable one. This is not a request to fetch now — it is that the round ranks its levers without ever pricing this one, while the proposal forbids sector data work and simultaneously relies on an authorized 741-session SPX collection. Diagnose the cause of the XLRE/XLU/XLC gaps before spending ten cells on lower-yield levers. |
| 5 | B05's "high-N challenger" rationale is contradicted by the same artifact the proposal reads its parent table from, and both framings are left standing | MEDIUM | internal-contradiction | Same retention evidence as #3: B05/B09 retention ratios across the 8 analogues are **0.88–1.10** (15.4 vs 14.0; 22.8 vs 21.8; 46.6 vs 52.8; 16.1 vs 17.8). Best available estimate for B05+S4 ≈ **105–125** entries and B05+F4 ≈ **85–100** — below the incumbent 235/188. The amendment's rebuttal ("retention can differ across parents; 1,537 parent entries do not cap filtered N below 235") is true in principle, but the directly relevant evidence in the cited file says retention is close to parent-invariant, and the response does not engage it. Meanwhile the amendment concedes B05's closest prior analogues were null (55.6% vs 55.6% baseline) and negative (52.2%), yet the main body still selects B05 because it "offers a larger pool of stalled-selloff entries" and calls it "the high-N challenger." Choose one: B05 as a deliberately skeptical falsification parent is a legitimate design — then drop the N rationale and the "high-N challenger" label, since the round's own evidence predicts it loses on N. |
| 6 | The SPX leg's expected participation is ≈2× F4's, and a free empirical proxy for it sits unused in the frozen data | MEDIUM | non-comparable-cohorts | The revision rejects the prior selectivity finding by assertion (M is zero-anchored, non-Gaussian — correct, and the reviewer's "fires on a large fraction of windows" was overstated). But the frozen sector data gives a direct proxy for a breadth-1 `M>1 AND b2<−1e−12` rule under the *identical* construction: per-ETF **9.5%–15.2%**, pooled **12.45%** — versus F4's 6.0%. The mechanism the reviewer named therefore holds at about 2×, not 10×. The pre-declared OR test will roughly double the qualifying set with a leg of about half the strictness, which is precisely the condition the proposal itself nominates as failure ("If added entries dilute accuracy… increasing N alone is not success"). Put the proxy in the plan, and pre-declare one participation-matched SPX cell alongside the fixed `M_SPX>1` rule — matching on pre-outcome participation is not outcome tuning and does not violate the no-threshold-search commitment. |
| 7 | The transfer largely re-tests the same IV windows, so "portability check" overstates what a positive result would show | MEDIUM | non-independence | Qualification is a pure function of (date, 30-minute window) sector IV — it does not depend on the price trigger at all. S4 already qualifies on **100 of 178** B09-active dates and F4 on 95, i.e. these are >50%-of-days selectors, and B05/B07 fire on the same dates and largely the same hour blocks as B09. A large share of transfer-qualifying entries will therefore be the *same IV windows* already used to pick M>1/breadth-4 out of 48 correlated cells; what varies is only the price trigger. Contract item 10 does require timestamp-overlap reporting, but frames it solely as an opportunity-count measure and never states the evidential consequence. Pre-declare the transfer readout restricted to date×hour blocks containing no qualifying B09 entry — that subset is the only genuinely new information the round produces about the rule, and it should be reported before the pooled number. |

**Verified correct while reviewing (not findings).** Every amended number reproduces exactly from primary artifacts: the outcome decomposition (plain 3141/1704/685/751/1; S4 235/139/61/35/0; F4 188/115/46/27/0; S5 80/53/18/9/0; F5 64/43/14/7/0) from `summary.csv`; all six uplift intervals (S4 −1.55/+11.35, −5.40/+16.21, −0.29/+15.77; F4 +0.013/+13.63, −1.08/+20.30, +2.75/+19.67) from `uncertainty.csv`; the B07/B05/B03 parent table and all ten prior-analogue cells from `comparison.csv`; 105 rule labels × 13 parents × 3 modes; the three SPX cache counts (1,065 / 22 / 31); 741 = 681 target + 60 warmup sessions. The construction claims hold empirically: M is −a/(1.4826·MAD) with historical\_median/scale within ±0.12 at the 99th percentile (so non-centering is immaterial), baseline support is deep (median 60 sessions, 0.27% below 30), and hour-block firing rates are stable at 12.7–15.1% (so the per-hour baseline works). The prior review's clock-parity concern is **resolved, not open**: B09 entries span minutes 600–870 (10:00–14:30 ET), the T−30…T−1 window needs 09:30–14:29, and the SPX collector's declared scope is exactly 09:30–14:29 — no structural entry-minute coverage gap exists. I did **not** re-raise the linear-payoff objection; the author's rejection of it is internally consistent with the stated first-move objective, and under that objective hit rate is the objective function, so S4's +4.9pp remains a valid description (with the adverse-share caveat the revision now prints). Status claims are accurate: `CLAUDE_STRATEGY_RECHECK.md` is empty and no approval is asserted.

**Suitability as exploratory research.** Yes, with conditions. The revision's disclosure, labelling and uncertainty reporting are now adequate and its arithmetic is clean; nothing here claims validation or deployment. Findings 1 and 2 should be fixed before the round runs — both are cheap (a breadth-matched or raw-magnitude control; per-half coverage columns plus one coverage-held-constant sensitivity), and without them two cells are wasted and all ten inherit a shared, non-independent artifact. Finding 3 needs one sentence added to item 9. Findings 4–7 are scope and framing corrections that change what the round is claimed to show, not whether it can be run.
