# Complete-cache warmup correction before outcomes

The first preparation used the original dashboard inventory, the new100-date
collection's normalized SPX cache, and the frozen150 evaluation source paths.
The diagnostic gap ledger exposed an apparent21-calendar-day gap before2026-04-01.
The existing full population ledger then identified35 valid native sessions in
the earlier50-day collection's normalized cache that were missing from this union.

All35 are read from existing immutable sources. No data download or outcome-based
choice is involved. Include every available source recorded by the full population
ledger through the original dashboard's last evaluated date,2026-09-08. The old
dashboard source inventory still supplies the separate original441-event reproduction.
The eight entry modules,150 evaluation dates, score, VT gate, bins method and all
other protocol choices are unchanged. Recompute smoothing and diagnostic cut points
on the complete declared history BEFORE any new-family outcome scoring.

The original namespace
`/Users/dgrissen/Dev/central_trade_data/thetadata/branch_b_150d_rerun_2026-09-19-v1/`
is preserved as an **unscored preparation** with7,413 raw candidate records. It has
no event_outcomes or comparison table and is not authoritative for the experiment.

The corrected authoritative namespace is
`/Users/dgrissen/Dev/central_trade_data/thetadata/branch_b_150d_rerun_2026-09-19-v2/`.
This overrides the v1 output location in PROTOCOL.md while preserving that frozen
document and all first-pass hashes. `complete_history.py` is the narrow source-
inventory adapter; the original runner remains byte-identical. Its own hash and
the population ledger/correction note are separately frozen before outcome scoring.

The source-inventory omission demonstrates why reproducing an old dashboard is
necessary but insufficient: a faithful old inventory can still omit subsequently
collected warmup observations. Every before/after identity change is retained in
the corrected event-ledger comparison; it must not be presented as indicator tuning.
