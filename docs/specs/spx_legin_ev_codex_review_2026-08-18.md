## Codex Review — Verdict: FAIL

**Scope**: Uncommitted changes: new analysis script and two regenerated Parquet outputs  
**Files reviewed**: 3

### Findings

| # | Finding | Severity | File:Line | Description |
|---|---------|----------|-----------|-------------|
| 1 | Trigger and baseline use different horizons | CRITICAL | scripts/spx_legin_ev.py:131 | `outcome()` evaluates exactly `N` future bars, but `spx_touch_stats_full.parquet` was generated by `spx_touch_stats.py` using only `N-1` bars. At the defaults, the reported baseline is 0.804246 instead of 0.806683, omitting 35 final-bar hits and silently overstating the trigger improvement. |
| 2 | Baseline is not clock-time matched | CRITICAL | scripts/spx_legin_ev.py:121 | Triggers occur at arbitrary minutes with a non-uniform intraday distribution, while the baseline uniformly samples 15-minute starts. This confounds the trigger with time-of-day effects and violates the specified “same clock minute” comparison. A trigger-clock-weighted baseline is 0.802683, demonstrating that the chosen sampling changes the estimate. |
| 3 | Output artifacts are not self-describing and contradict the trading document | CRITICAL | scripts/spx_legin_ev.py:124 | Fixed filenames are overwritten for every parameter combination, while the trigger output omits `x_bp`, `window`, `pull` threshold, and `stall_bars`; the chop output also omits `x_bp`. The regenerated trigger artifact now contains 7,337 fires and a 0.237 low-break rate, while §2c still reports 8,353 and 0.211. Consumers cannot detect either stale documentation or non-default output semantics. |
| 4 | Accepted CLI values can fail after overwriting outputs | HIGH | scripts/spx_legin_ev.py:135 | Arguments are not validated against downstream assumptions. `--pull 12` produces duplicate bin edges and crashes; values above 12 produce non-monotonic bins. Unsupported `--x-bp`/`--window` values silently produce an empty baseline and `nan` statistics, while a pull threshold yielding no fires causes `r.up_hit` to fail. These errors occur after the canonical Parquet files have already been replaced. |
| 5 | Prior-range windows contain one extra bar | MEDIUM | scripts/spx_legin_ev.py:98 | `hi[i0-w:i0+1]` and `lo[...]` contain 31/61 bars for nominal prior-30/60-minute ranges. Although 61 closes are appropriate for a 60-return ER calculation, the OHLC range should cover 60 one-minute bars. The extra bar changes 21% of `rng60` observations and shifts the quartile boundaries. |
| 6 | Zero-return states are counted as direction flips | MEDIUM | scripts/spx_legin_ev.py:100 | `(np.diff(r5) != 0)` treats `+1 → 0 → -1` as two flips and `+1 → 0 → +1` as two despite no direction reversal. In the generated sample this affects 995 of 12,675 feature windows, so the flip quartiles do not strictly represent direction changes. |
| 7 | Adverse-path metrics violate their stated semantics | MEDIUM | scripts/spx_legin_ev.py:44 | `mae_up` is not clamped at zero, producing negative “adverse excursions” for 494 of 7,337 trigger rows. Additionally, `seg` includes the low of the bar that first touches the up target; OHLC cannot establish whether that low occurred before or after the target high. Consequently `mae_up` and `broke_low` sometimes claim a pre-touch event without known intrabar ordering. |

### Dimension Summary

| Dimension | Rating |
|-----------|--------|
| API correctness | Pass |
| Logic errors | Fail |
| Data contracts | Fail |
| Edge cases | Fail |
| Silent failures | Fail |
| Security | Pass |
