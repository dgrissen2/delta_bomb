| # | finding | severity | fix |
|---|---|---|---|
| 1 | Entry/exit ownership is contradictory. `rules.py` claims R4–R7 and emits exits, while `executor.py` also “checks exits.” The loop passes the signal bar to `executor.apply`, yet R1.4 requires entry at the next bar’s open. No pending-order state exists. | Blocker | Make `RuleEngine` own condition evaluation and exit precedence; add `PendingEntry`; execute it before evaluating the new bar, with S0 equal to that bar’s open. Define one owner for every R7 rule. |
| 2 | The data model cannot implement R1/R7 exactly. It lacks expiry, leg strikes, chain quote timestamp, entry option price, resting-limit reference, cap source/value, resolution debit, and Branch-A BH/reference-low metadata. Crash reconstruction from an entry row is therefore incomplete. | Blocker | Expand `PendingEntry`, `SimTrade`, and persisted events with all execution and recovery inputs; test round-trip reconstruction. |
| 3 | R2–R3 coverage is incomplete: `Bar` has no volume for SPY VWAP; no event-calendar component; no explicit levels/IM loader; no 09:30 open, EMA, VWAP, or retained 10:30/13:00 context fields; pooled causal `range60_pct` history is not sourced. | Blocker | Define typed SPX/SPY inputs and dedicated levels/calendar loaders; specify causal historical initialization and retained context state. |
| 4 | R13 price tier is not designed. `--tier selects sources` does not encode price-A’s altered R6.1, disabled Branch B/R4.3, altered R7.2, or mandatory `tier=price` isolation. | Blocker | Define an explicit immutable tier policy consumed by features/rules, with all R13.1 differences enumerated and tested. |
| 5 | Degraded mode is internally wrong. “SPX down — same shape” cannot continue spot exits without bars; recovery messages/outage spans, `degraded_vwap`, early-ending PARTIAL, and missing-input-specific exit behavior are unspecified. | Major | Add a session health state machine defining stall detection, recovery, cumulative duration, unavailable exits, and final PARTIAL classification. |
| 6 | R8’s event schema is too generic to guarantee the required log. `features_subset` and `outcome` hide mandatory columns; session status, shakedown, heartbeat state, strike quotes, cap path, and outage details lack contracts. | Major | Use explicit versioned event fields and event-type validation; distinguish `mode=shakedown` as required and persist session disposition. |
| 7 | R9/R11 are asserted, not designed: qualifying episodes versus executable entries, censored denominators, A-over-B deduplication, weighted controls, best-session removal tie-breaks, and rehearsal/live isolation have no stated algorithm. | Blocker | Specify the scorecard aggregation pipeline and intermediate records criterion-by-criterion. |
| 8 | R13.3 is incomplete: summaries are attached mainly to sweeps, with no bootstrap implementation, own-dataset controls, eligibility filtering, or deterministic leaderboard contract. | Major | Add one shared summarizer for backtest and sweep, including 2,000 day-clustered draws at seed 42. |
| 9 | Tests miss the highest-risk surfaces: next-open timing, session boundaries, every exit precedence pair, chain/proxy paths, price tier, calendar/levels/VWAP degradation, scorecard denominators/re-check, config isolation, and console/CSV identity. | Major | Add table-driven boundary tests plus golden scorecard, tier, degradation, and serialized-event tests. |

Verdict: **FAIL**
