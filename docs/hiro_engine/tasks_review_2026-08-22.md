| # | Finding | Severity | Fix |
|---|---|---|---|
| 1 | R1.1–R1.3 are orphaned. No task owns selecting the 20–40 DTE expiry, choosing the closest −0.20-delta put, constructing the 5-wide vertical, or enforcing one-contract paper sizing. Task 7 only supplies chain data. | Blocker | Add a single owner—preferably an `InstrumentSelector` task before Executor—and explicit tests for expiry, delta tie-breaking, width, and size. |
| 2 | The listed Event schema cannot persist every SimTrade field required by task 5. It omits at least `resting_limit_ref`, `target`, `bh_level`, `entry_L`, `cap_value`, and trade `state`. Because catch-all fields are forbidden, field-for-field crash recovery is impossible as specified. | Blocker | Expand the explicit v1 schema and add a test proving every SimTrade field survives ENTRY/EXIT serialization and replay. |
| 3 | Session orchestration is unowned. The design says CLI “builds Session,” but no task defines Session or the per-bar ordering between feed, pending-open execution, feature calculation, rule evaluation at close, logging, and health transitions. | Blocker | Add a Session task before task 6 with an explicit lifecycle contract and ordering tests. |
| 4 | Tier ownership is out of dependency order. Task 6 exposes `--tier`, while TierPolicy and price-tier behavior do not exist until task 9. Task 2 also hardcodes ReplayFeed as full-tier. | Major | Implement TierPolicy before ReplayFeed/CLI, or remove `--tier` from task 6 and introduce it only in task 9. State clearly that the golden gate uses full tier. |
| 5 | R8.1 logging coverage is incomplete/ambiguous. Signal, veto and exit events are named, but `skip`, armed-episode gate failure, and periodic state-line production have no explicit owner or test matrix. | Major | Assign all event-generation conditions to RuleEngine and require schema/formatter tests for every R8.1 event type and reason. |
| 6 | FeatureRow ownership conflicts with the component boundaries: it contains `vetoes` and `health`, although veto logic belongs exclusively to RuleEngine and health belongs to Session. A junior must guess who populates them. | Major | Remove those fields from FeatureRow or explicitly define them as immutable inputs attached by Session, not calculated by FeatureEngine. |
| 7 | Task 10 runs live shakedowns before task 11 creates the morning/evening operational checks and manifest validation those sessions depend on. | Major | Move task 11 before shakedown execution; keep final integration and acceptance afterward. |
| 8 | Task 10’s test gate is not independently reproducible: “run two live shakedown sessions” lacks pass criteria and depends on market availability. | Minor | Separate deterministic readiness tests from the operational acceptance gate and define required artifacts and pass/fail conditions. |

Verdict: **FAIL**
